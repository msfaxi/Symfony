
; Project: UGAP - {{ ucprojectname}}
; @package {{ ucprojectname}}-CONFIG


; ==================================================================
[secret]
; ==================================================================

; ------------------------------------------------------------------
; Application {{ ucprojectname}}, pilote IHM
; ------------------------------------------------------------------

; Clé secrète pour chiffrer les variables de session du pilote
session.key     = "this.is.a.dangerous.password"

; TTL du cookie (en seconde), ou zéro pour durée de vie du navigateur
session.timeout = 900

; Authentification HTTP/Basic du pilote
{{ projectname}}.pilote.identity = "{{ projectname}}.pilote"
{{ projectname}}.pilote.password = "{{ projectname}}.password"

; Clé de crypt/decrypt du mot de passe pour l'authentification entre le pilote et le silo
{{ projectname}}.password.key = "{{ projectname}}.key"

; Email de l'administrateur de l'application
email.admin = "jparneodo@gmail.com"

; Email de l'administrateur d'envoi de mail automatique
email.{{ projectname}} = "pierre.jean.1994@gmail.com"

; Paramètres smtp de connexion
; Valeurs possibles pour le champ type: none | ssl | tls
smtp.host     = "smtp.gmail.com"
smtp.port     = "587"
smtp.type     = "tls"
smtp.username = "pierre.jean.1994@gmail.com"
smtp.password = "isis1994"


; ==================================================================
[solr]
; ==================================================================

login       = "Hello"
password    = "world!"

